from setuptools import setup

setup(name='Uwu',
      version='0.0.1',
      author='Zai',
      packages=['uwu'],
      description='A text to speech and speech to text model',
      license='GNU',
      install_requires=[
            'torch','torchaudio'
      ],
)
